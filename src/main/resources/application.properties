# DynamoDB Configuration
aws.dynamodb.endpoint=http://localhost:8000
aws.dynamodb.region=us-east-1
aws.dynamodb.accessKey=dummy
aws.dynamodb.secretKey=dummy
server.port=8080

# Environment Configuration
# Table name prefix for environment isolation (e.g., dev-DocumentMetadata, prod-DocumentMetadata)
app.environment.prefix=local

# Spring Application
spring.application.name=validate-spring-data-dynamodb
